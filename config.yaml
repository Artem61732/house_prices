# config.yaml

paths:
  train: "data/train.csv"
  test: "data/test.csv"
  submission: "submission.csv"

random_state: 42

cv:
  n_splits: 5

tune:
  n_trials: 30

preprocess:
  skew_threshold: 0.75

blend:
  weights:
    catboost: 0.30
    ridge:    0.45
    lightgbm: 0.25
