# Веса blend — ручные, не из Optuna (см. ml/create_submission.py).
# Гиперпараметры моделей — outputs/ml/best_params.json (ml/tune.py).

blend:
  weights:
    catboost: 0.30
    ridge:    0.45
    lightgbm: 0.25

tune:
  n_trials: 30
